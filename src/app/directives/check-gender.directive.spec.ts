import { CheckGenderDirective } from './check-gender.directive';

describe('CheckGenderDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckGenderDirective();
    expect(directive).toBeTruthy();
  });
});
